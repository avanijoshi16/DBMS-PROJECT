# DBMS-PROJECT
# Library Management System

Simple DBMS mini project using:
- SQL (database)
- Java (JDBC console app)

## How to Run
1. Run `database.sql` in MySQL
2. Update password in `LibrarySystem.java`
3. Compile & run Java file

## Features
- Add books
- Issue books
- Return books
